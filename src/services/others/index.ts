import { EagerLoadable } from "../../shared/implementations/eagerload.class";

export default class Other implements EagerLoadable {
    constructor() {

    }

    eagerLoad(): void {
        
    }
}